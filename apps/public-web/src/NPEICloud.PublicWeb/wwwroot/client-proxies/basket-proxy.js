/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module basket

(function(){

  // controller NPEICloud.basketService.basket

  (function(){

    abp.utils.createNamespace(window, 'NPEICloud.basketService.basket');

    NPEICloud.basketService.basket.get = function(anonymousUserId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/basket/basket' + abp.utils.buildQueryString([{ name: 'anonymousUserId', value: anonymousUserId }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    NPEICloud.basketService.basket.addProduct = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/basket/basket/product',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    NPEICloud.basketService.basket.removeProduct = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/basket/basket/product' + abp.utils.buildQueryString([{ name: 'productId', value: input.productId }, { name: 'count', value: input.count }, { name: 'anonymousId', value: input.anonymousId }]) + '',
        type: 'DELETE'
      }, ajaxParams));
    };

  })();

})();


